version: '3'
services:
  cloudflareddns:
    container_name: cloudflareddns
    pull_policy: build
    build: .
    restart: unless-stopped
    environment:
      - ZONE=${ZONE}
      - RECORD=${RECORD}
      - TOKEN=${TOKEN}
      - NODE_ENV=${NODE_ENV}